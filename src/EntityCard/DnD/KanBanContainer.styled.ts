import styled from 'styled-components'

export const KanBan = styled.section`
  display: flex;
  flex-direction: row;
  gap: 8px;
  padding: 8px;
  padding-bottom: 0;
  overflow-x: auto;
  overflow-y: hidden;
  overflow-x: overlay;
  width: min-content;
  position: absolute;
  inset: 0;
`
